export interface ICategoryProps {
    url: string;
    title: string;
    categoryName: string;
}

export const enum ProductType {
    PHONE = 'phone',
    LAPTOP = 'laptop',
    WATCH = 'watch',
    TABLET = 'tablet',
}
